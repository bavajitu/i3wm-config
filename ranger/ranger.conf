# Ranger configuration for Nordic theme

# Set the default file previewer to use
set preview_images true
set preview_images_method w3m

# Custom colors for Nordic theme
set colorscheme nordic
set highlight_active true
set show_hidden true
set show_dotfiles true

# Key bindings
map <C-n> shell nnn # Use nnn as an alternative file manager

